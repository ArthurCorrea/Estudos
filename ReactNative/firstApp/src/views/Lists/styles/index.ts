import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        // justifyContent: 'center',
        alignItems: 'center',
    },
    viewList: {
        width: '100%',
        height: 40,
        backgroundColor: '#fff',
        borderWidth: 2,
        marginTop: 5,
        marginBottom: 5,
    }
})